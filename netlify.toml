# TanStack Start (SSR) on Netlify.
# The Nitro "netlify" preset (enabled in vite.config.ts when NETLIFY is set)
# emits static assets to dist/ and the SSR handler to
# .netlify/functions-internal/server, which Netlify picks up automatically.
[build]
  command = "bun run build"
  publish = "dist"
